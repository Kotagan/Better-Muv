<Window x:Class="BetterMuv.FirstRunNoticeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Better-Muv 提示"
        Width="520" SizeToContent="Height"
        WindowStyle="None" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="#202630" Foreground="#F5F7FA"
        FontFamily="Microsoft YaHei UI"
        Closing="Window_Closing">
    <Border BorderBrush="#3A4552" BorderThickness="1" Padding="28,24">
        <StackPanel>
            <TextBlock Text="首次运行提示" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16" />
            <TextBlock x:Name="MessageText"
                       FontSize="15"
                       LineHeight="26"
                       TextWrapping="Wrap"
                       Margin="0,0,0,22" />
            <Button x:Name="CloseButton"
                    Content="我知道了（10）"
                    IsEnabled="False"
                    HorizontalAlignment="Right"
                    MinWidth="140"
                    Height="36"
                    Padding="18,0"
                    Cursor="Hand"
                    Click="CloseButton_Click"
                    Foreground="White"
                    Background="#3A4552"
                    BorderThickness="0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="7"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#2196E8" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.55" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Border>
</Window>
